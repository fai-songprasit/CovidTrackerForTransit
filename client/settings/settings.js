import {ProgramModeEnum} from "../classes";

/**
 * Settings includes the current program mode - change between different program modes to deal with test prod data
 */
const settings ={
    MODE: ProgramModeEnum.PROD
};

export {settings};